
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	/* 2) Descreva o protótipo da função:
? fgetc = le letra por letra do arquivo; 
? fgets = le uma pralavra inteira do arquivo;
? feof = fim end of FILE fim de arquivo;
? fscanf = leitura de arquivo;
? fopen = obertura de arquivo;
? fprintf = escrever no arquivio;
? strcmp = funçao de comparar strings; 
	
	4) A.20
		b.15
		
    5) A

	6) Dado o programa abaixo, qual seria a condição correta para o lao de repeticção
	 executar enquanto a opçcão digitada seja “sim”
A) while (strcmp(opcao,"sim") ==0 )
	
	8) Dado o programa abaixo, o que é executado

B) Troca todas as vogais minúsculas por suas correspondentes maiúsculas;

	9) Referente a vetores e matrizes, qual afirmação está correta:
A) Um vetor ou uma matriz é uma coleção de variáveis de mesmo tipo,
acessíveis com um único nome e armazenados contiguamente na memória.
A diferença está que a matriz é bidimensional e o vetor é unidimensional;

	10) Transcreva os cálculos abaixo em linguagem de programação:
a) 2 * 4 = 24 / 3 + 5; 				 (2*4)= (24/3)+5
b) 15 - 4 > 19 + 6;       			 15-4> 19+6
c) 2 + 8 * 7 <= 3 * 6 – 15; 		 2+(8*7) <=(3*6)-5
d) 2 < 5 AND 15 / 3 = 3 ;   		 2< 5 && (15/3) = 3
e) NÃO (35 / 5 < 24) OR (7 * 2 = 20 mod 3);				!-(35/5<24) || ((7*2)=20 ?? 3
f) 45 < 54 OR 45 > 54 AND Verdadeiro Or 20 / 6 >= 4 	??

	
*/ 

	return 0;
}
